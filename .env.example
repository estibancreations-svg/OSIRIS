# Optional. Defaults to the verified public OSIRIS deployment until the
# Estibancreations controlled runtime is deployed.
OSIRIS_BASE_URL=https://osirisai.live

# Supabase provides SUPABASE_URL and backend keys to deployed Edge Functions.
# Do not commit populated secret values.
